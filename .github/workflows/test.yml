on:
  pull_request:
    branches:
      - test
  push:
    branches:
      - test
name: 🚀 Deploy website on pull request on main
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.1.0
      with:
        server: ${{ secrets.FTP_SERVEROVH }}
        username: ${{ secrets.FTP_USEROVH }}
        password: ${{ secrets.FTP_PASSWORDOVH }}
        server-dir: /web/tests/
